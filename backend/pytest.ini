[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts =
    -v
    --tb=short
    --strict-markers
markers =
    integration: Integration tests (require running Lambda container)
    unit: Unit tests (no external services)
    slow: Slow tests
